let a =[1,2,3,4,5,3]
function removeDuplicates() {
    let duplicatenumber = a.filter((value, index) => a.indexOf(value) === index);
return duplicatenumber;
}
let result = duplicatenumber;
console.log(result);

removeDuplicates();